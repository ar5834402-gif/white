## Packages
framer-motion | Animations for the timeline, hearts, and page transitions
lucide-react | Beautiful icons for the UI
react-hook-form | efficient form handling
zod | schema validation
@hookform/resolvers | zod resolver for react-hook-form
canvas-confetti | For coupon redemption effects
@types/canvas-confetti | Types for confetti
date-fns | Date formatting
wouter | Routing (already in base, but noting usage)
clsx | Class name utility
tailwind-merge | Class name merging

## Notes
- Images and Music are uploaded via standard file inputs. 
- The backend handles file uploads and serves them.
- Girlfriend view requires a specific password check before unlocking content.
- Using Google Fonts: 'Playfair Display' (serif headings), 'Dancing Script' (handwritten notes), 'Lato' (body).
